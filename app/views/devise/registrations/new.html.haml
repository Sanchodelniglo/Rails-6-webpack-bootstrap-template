.d-flex.flex-column.justify-content-center.align-items-center

  %h2 S'inscrire
  = form_for(resource, as: resource_name, url: registration_path(resource_name), defaults: { required: true }) do |f|
    = render "devise/shared/error_messages", resource: resource
    .field
      = f.label 'Prénom'
      %br/
      = f.text_field :first_name, autofocus: true, autocomplete: "first-name"
    .field
      = f.label 'Nom'
      %br/
      = f.text_field :last_name, autofocus: true, autocomplete: "last_name"
    .field
      = f.label :pseudo
      %br/
      = f.text_field :pseudo, autofocus: true, autocomplete: "pseudo"
    .field
      = f.label :email
      %br/
      = f.email_field :email, autofocus: true, autocomplete: "email"
    .field
      = f.label 'Mot de passe'
      - if @minimum_password_length
        %br
        %em
          (#{@minimum_password_length} characters minimum)
      %br/
      = f.password_field :password, autocomplete: "new-password"
    .field
      = f.label 'Confirmation mdp'
      %br/
      = f.password_field :password_confirmation, autocomplete: "new-password"
    .actions.mt-2.mb-2.text-center
      = f.submit "S'inscrire", class: 'btn btn-primary'
  = render "devise/shared/links"
